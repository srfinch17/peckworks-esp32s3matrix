Saved matrix expressions (JSON) created via matrix_animate save_as.
Committed to git so good drawings survive and sync across machines.
Format matches mcp_server/expressions.ts Expression: { description, frame_ms, loop, colors, frames }.
